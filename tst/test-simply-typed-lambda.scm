(define add2 (lambda (int) int) (lambda (n) (+ n 2)))
(define 
(define bad (lambda (int) int) (lambda (n) (+ n "growl")))
(define two int 2)

(define integer type)
(define z integer)
(define s (lambda (integer) integer))

(define bool type)
(define true bool)
(define false bool)
