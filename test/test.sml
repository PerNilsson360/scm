1 + 1;
1 = 1;
val one = 1;
val id = fn x => x;
val add_two = fn x => x + 2;
val mul_two = fn x => x * (3 - 1);
val is_two = fn x => if x = 2 then 1 else 0;
val rec fac  = fn x => if x = 1 then 1 else x * fac (x - 1);

